# HW_OOP_2808
Add to the string class a function that creates a string containing the intersection of two strings, that is, common characters for the two strings.
For example, the result of the concatenation of the strings "sdqcg" "rgfas34" will be the string "Sg".
To implement the function, overload the operator "*"
